# 心海记忆系统 · 部署样例

> 一份「如果你也想搭」的最小参考。按自己家的情况改地址和钥匙。

## 最小三件套

```text
记忆库（Ombre-Brain）  -> 18001  存每句值得记住的话
情绪海（心潮类服务）   -> 18110  实时心思潮汐 + 梦
自省室（年轮类脚本）   -> 每天22:00 渲染房间散文并回写记忆库
```

## docker-compose 参考

```yaml
services:
  ombre-brain:
    image: 你的记忆库镜像
    ports:
      - "127.0.0.1:18001:8000"
    volumes:
      - ./buckets:/app/buckets
    env_file: .env
    restart: unless-stopped

  dynamic-mind:
    image: 你的情绪服务镜像
    ports:
      - "127.0.0.1:18110:18110"
    env_file: .env
    restart: unless-stopped
```

## .env 参考（钥匙自己生成，别照抄）

```bash
# 记忆库
OMBRE_API_KEY=openssl-rand-hex-32
# 情绪服务与记忆库互通
DYNAMIC_MIND_TOKEN=openssl-rand-hex-32
DYNAMIC_MIND_URL=http://dynamic-mind:18110
# 网页可视化（可选）
DASHBOARD_ENABLED=true
DASHBOARD_ACCESS_TOKEN=openssl-rand-hex-32
DASHBOARD_INCLUDE_PRIVATE_TEXT=false
# 桥（可选）
BRIDGE_ENABLED=true
BRIDGE_MACHINE_TOKEN=openssl-rand-hex-32
```

## 常驻差事

```bash
# 信差：小屋来信 -> 记忆库（每分钟）
tmux new-session -d -s mailman 'python3 scripts/mailman.py'

# 梦差：梦 -> 记忆库（每10分钟）
tmux new-session -d -s dreamman 'python3 scripts/dreamman.py'
```

## 安全三原则

1. 钥匙一律 chmod 600 放服务器，不进仓库；
2. 私密文本（梦、信）默认不走公网；
3. 先备份再动地基。
